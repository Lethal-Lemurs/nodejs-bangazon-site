if session.passport
  span #{session.passport.user.username}
  form(method="POST")
    button(type="submit" formaction="/logout") logout

    ul
      |
      |
      select(class="selectType" name="selectType" onchange="location = this.value;") 
        option Select a product category!
        option(value='/productTypes') Product Categories
        if locals.prodTypes
          each prodType in locals.prodTypes
            -let url = `/getProductsByTypes/` + prodType.name;
            option(value=url) #{prodType.name}
      |
      |
      a(href="/postProduct") Sell Product
      |
      |
      a(href='/userProfile') Profile
      |
      |
      a(href='/viewCart') View Cart
      |
      |

if session.passport
  form(action="/products", method="GET")
    input(type="text", name="search")
    button#button(type="submit") Search
  br


